extends layout
block content
    div.text-center
        form(class="form-signin" method="POST" action="/signup")
            
            - var h1Classes = ['h3', 'mb-3', 'font-weight-normal']
            h1(class=h1Classes) Create User Here

            label( for="inputEmail" class="sr-only mb-3") Email address
            input(type="email" name="email" id="inputEmail" class="form-control" placeholder="Email address" required autofocus)

            label(for="inputPassword" class="sr-only mb-3") Password
            input(type="password" name="password" id="inputPassword" class="form-control" placeholder="Password" required)

            - var buttonClass=['btn', 'btn-lg', 'btn-primary', 'btn-block', 'mb-3'];
            button(class=buttonClass type="submit") Create
            